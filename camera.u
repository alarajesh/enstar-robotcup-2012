loadModule("CamDriver/CamDriver.so");
var cam = CamDriver.new(0);

loadModule("ImageViewer/ImageViewer.so");
var viewer = ImageViewer.new("Camera");

var connector = viewer.&inputImage << cam.&outputImage;
